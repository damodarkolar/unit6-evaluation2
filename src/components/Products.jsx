import React from "react";

const Products = () => {
  return <div>{
    const data=useEffect(()=>{
      fetch(``)
      .then((data)=> {data.json()})
      .then(()=> {console.log(data)})
      .catch()
    },[])

    }</div>;
};

export default Products;
